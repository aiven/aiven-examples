node.environment=production
node.id=trino
# The official trinodb/trino image runs as the "trino" user and only /data/trino
# is writable; pointing elsewhere (e.g. /opt/trino/data) fails with "permission denied".
node.data-dir=/data/trino
plugin.dir=/usr/lib/trino/plugin
